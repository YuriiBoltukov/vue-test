export enum Title {
  date = 'date',
  origin = 'origin',
  destination = 'destination',
  airline = 'airline',
  departure_at = 'departure_at',
  return_at = 'return_at',
  expires_at = 'expires_at',
  price = 'price',
  flight_number = 'flight_number',
  transfers = 'transfers'
}
